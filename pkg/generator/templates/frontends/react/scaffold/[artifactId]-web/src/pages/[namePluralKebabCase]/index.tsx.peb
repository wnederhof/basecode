import { {{namePascalCase}}List } from '@components/app/{{nameKebabCase}}/{{nameKebabCase}}-list'
import { DefaultLayout } from '@components/layout/DefaultLayout'
import { Actions } from '@components/ui/actions'

export default function {{namePluralPascalCase}}() {
  return (
    <DefaultLayout
      breadcrumbs={[
        { label: '{{namePluralPascalCase}}', href: '/{{namePluralKebabCase}}' },
      ]}
    >
      <Actions>
        <Actions.Action href="/{{namePluralKebabCase}}/new">New</Actions.Action>
      </Actions>
      <{{namePascalCase}}List />
    </DefaultLayout>
  )
}
