import { ReactNode } from 'react'

interface Props {
  title: string
  visible: boolean
  children: ReactNode
}

export const Modal = (props: Props) => (
  <>
    {props.visible && (
      <>
        <h1>{props.title}</h1>
        {props.children}
      </>
    )}
  </>
)
