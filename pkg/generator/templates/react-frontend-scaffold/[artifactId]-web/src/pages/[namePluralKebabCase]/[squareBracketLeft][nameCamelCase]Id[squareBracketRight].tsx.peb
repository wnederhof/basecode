import { {{namePascalCase}}Details } from '@components/{{nameKebabCase}}/{{namePascalCase}}Details'
import { useRouter } from 'next/router'

export default function {{namePascalCase}}Id({ id }: Props) {
  const router = useRouter()
  const { {{nameCamelCase}} } = router.query
  return <{{ namePascalCase }}Details id={{ "{" }}{{ nameCamelCase }}Id{{ "}" }} />
}
