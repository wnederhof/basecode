import { {{namePascalCase}}Form } from '@components/app/{{nameKebabCase}}/{{namePascalCase}}Form'

export default function New{{namePascalCase}}() {
  return <{{ namePascalCase }}Form
    onSave={() => history.back()}
    onCancel={() => history.back()}
  />
}
