import { {{namePascalCase}}Form } from '@components/app/{{nameKebabCase}}/{{namePascalCase}}Form'
import { DefaultLayout } from '@components/layout/DefaultLayout'

export default function New{{namePascalCase}}() {
  return (
    <DefaultLayout
      title="New {{ namePascalCase }}"
      breadcrumbs={[
        { label: '{{namePluralPascalCase}}', href: '/{{namePluralCamelCase}}' },
        { label: 'New', href: '/{{namePluralCamelCase}}/new' },
      ]}
    >
      <{{ namePascalCase }}Form
        onSave={() => history.back()}
        onCancel={() => history.back()}
      />
    </DefaultLayout>
  )
}
