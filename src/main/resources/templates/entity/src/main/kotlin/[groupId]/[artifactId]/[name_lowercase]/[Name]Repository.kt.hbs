package {{ groupId }}.{{ artifactId }}.{{ name_lowercase }}

import org.springframework.data.repository.CrudRepository

interface {{ Name }}Repository : CrudRepository<{{ Name }}, Int>{{#if hasRelations}} {
{{#each fields}}{{#if isFieldRelational}}
    fun findBy{{ fieldNamePascalCase }}({{ fieldNameCamelCase }}: Int): List<{{ Name }}>
{{/if}}{{/each}}
}{{/if}}
