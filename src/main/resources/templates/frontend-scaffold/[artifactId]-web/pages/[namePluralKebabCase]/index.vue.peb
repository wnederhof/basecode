<template>
  <div>
    <VPageHeader>
      {{namePascalCase}} Details
    </VPageHeader>
    <{{namePascalCase}}List />
  </div>
</template>

<script lang="ts">
import {{ namePascalCase }}List from '@/components/{{ nameKebabCase }}/{{ namePascalCase }}List.vue'
import VPageHeader from '@/components/VPageHeader.vue'

export default {
  components: { {{ namePascalCase }}List, VPageHeader }
}
</script>
