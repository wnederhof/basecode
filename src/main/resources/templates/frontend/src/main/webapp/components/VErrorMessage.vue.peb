<template>{%if usesBootstrapComponents%}
  <BAlert variant="danger">{{ "{{" }} message {{ "}}" }}</BAlert>
{% else %}
  <p>{{ "{{" }} message {{ "}}" }}</p>
{% endif %}</template>

<script lang="ts">
import { Vue } from 'vue-property-decorator'
import Component from 'nuxt-class-component'

@Component({
    message: [String],
})
export default class VErrorMessage extends Vue {

}
</script>
