package {{ groupId }}.{{ artifactId }}.{{ nameLowerCase }}

import java.time.LocalDate
import java.time.LocalDateTime

object {{ namePascalCase }}Fixtures {

    val {{ nameScreamingSnakeCase }}_FIXTURE_1 = {{ namePascalCase }}(
        id = null{{#each fields}},
        {{ fieldNameCamelCase }} = {{{ fieldKotlinTestDummyValue }}}{{/each}}
    )

    val CREATE_{{ nameScreamingSnakeCase }}_FIXTURE_1 = {{ namePascalCase }}.Create{{ namePascalCase }}Input({{#each fields}}
        {{ fieldNameCamelCase }} = {{{ fieldKotlinTestDummyValue }}},{{/each}}
    )

    val UPDATE_{{ nameScreamingSnakeCase }}_FIXTURE_1 = {{ namePascalCase }}.Update{{ namePascalCase }}Input({{#each fields}}
        {{ fieldNameCamelCase }} = {{{ fieldKotlinTestDummyValue }}},{{/each}}
    )

    val {{ nameScreamingSnakeCase }}_FIXTURE_WITH_ID_1 = {{ nameScreamingSnakeCase }}_FIXTURE_1.copy(id = 1)

}
