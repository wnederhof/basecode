package {{ groupId }}.{{ artifactId }}.{{ names }}
{{#each fields}}{{#if fieldRelationship}}
import {{ groupId }}.{{ artifactId }}.{{ fieldTypes }}.{{ FieldType }}Event.Before{{ FieldType }}DeletedEvent{{/if}}{{/each}}
import org.springframework.context.event.EventListener
import org.springframework.stereotype.Component

@Component
class {{ Name }}Listener{{#if hasRelations}}(
    private val {{ name }}Service: {{ Name }}Service
) {
{{#each fields}}{{#if fieldRelationship}}
    @EventListener
    fun deleteBefore{{ FieldType }}Deleted(event: Before{{ FieldType }}DeletedEvent) {
        {{ name }}Service.deleteBy{{ FieldName }}(event.id)
    }
{{/if}}{{/each}}
}{{/if}}
