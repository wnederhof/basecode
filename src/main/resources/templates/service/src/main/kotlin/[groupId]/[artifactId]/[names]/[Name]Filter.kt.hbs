package {{ groupId }}.{{ artifactId }}.{{ names }}

data class {{ Name }}Filter(
    val id: Int? = null{{#each fields }}{{#if fieldRelationship}},
    val {{ fieldName }}: {{ FieldKotlinTypeNotNullable }}? = null{{/if}}{{/each}}
)
