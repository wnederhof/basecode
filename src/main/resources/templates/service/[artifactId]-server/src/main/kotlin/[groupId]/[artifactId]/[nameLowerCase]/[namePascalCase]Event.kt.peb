package {{ groupId }}.{{ artifactId }}.{{ nameLowerCase }}

import org.springframework.context.ApplicationEvent

sealed class {{ namePascalCase }}Event(source: Any) : ApplicationEvent(source) {

    data class {{ namePascalCase }}CreateEvent(private val _source: Any, val entity: {{ namePascalCase }}) : {{ namePascalCase }}Event(_source)

    data class {{ namePascalCase }}UpdateEvent(private val _source: Any, val entity: {{ namePascalCase }}) : {{ namePascalCase }}Event(_source)

    data class {{ namePascalCase }}DeleteEvent(private val _source: Any, val entity: {{ namePascalCase }}) : {{ namePascalCase }}Event(_source)

}
