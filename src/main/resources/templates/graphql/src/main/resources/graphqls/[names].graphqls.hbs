extend type Query {
    {{ name }}ById(id: Int): {{ Name }}
    all{{ Names }}: [{{ Name }}!]!
}

extend type Mutation {
    save{{ Name }}({{ name }}: {{ Name }}Input!): {{ Name }}!
    delete{{ Name }}ById(id: ID!): Boolean
}

{{#each fields }}{{#if fieldRelationship}}extend type {{ FieldType }} {
    {{ names }}: [{{ Name }}!]!
}
{{/if}}{{/each}}
type {{ Name }} {
    id: ID!{{#each fields }}
    {{ fieldName }}: {{ FieldGraphQLType }}{{/each}}{{#each fields }}{{#if fieldRelationship}}
    {{ fieldType }}: {{ FieldType }}{{/if}}{{/each}}
}

input {{ Name }}Input {
    id: ID{{#each fields }}
    {{ fieldName }}: {{ FieldGraphQLType }}{{/each}}
}
